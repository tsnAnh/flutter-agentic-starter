name: project_setup
description: Local setup wizard for flutter-agentic-starter.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.7.0
  yaml_edit: ^2.2.4

executables:
  project_setup:
